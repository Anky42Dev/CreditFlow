[pytest]
; DOC 5 §7.1: scoring_service is tested independently of backend-senior's
; pytest-django suite (own rootdir/ini, no DJANGO_SETTINGS_MODULE, no shared
; conftest.py) — run via `cd scoring_service && pytest`.
python_files = test_*.py
addopts = -v
